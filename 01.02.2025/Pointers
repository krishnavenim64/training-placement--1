#include <stdio.h>

int main() {

int num;

int *ptr = &num;

printf("Enter a number: ");

scanf("%d", ptr); // Use the pointer to read the value directly into num

printf("Value of the variable using pointer is: %d\n", *ptr);

return 0;

}
